# 🚀 Быстрый старт - Импорт тестов

## ✅ Что готово

- Subject "Забони тоҷикӣ" (ID: 1) останется без изменений
- Будут удалены только старые Topics и Tasks
- Импортируются новые правильные данные с категориями из PDF

## 📊 Результат

После импорта будет:
- **7 Topics** (правильные категории)
- **~1159 Tasks** (тесты с правильными ответами)

## 🚀 Запуск

```bash
# Перейдите в директорию Django проекта
cd /home/osimi/Рабочий\ стол/projects/hushyor

# Запустите команду
python manage.py clean_and_import_tests
```

## 📝 Процесс

1. Покажет сколько Topics и Tasks будет удалено
2. Попросит подтверждение (введите `yes`)
3. Удалит старые данные
4. Импортирует новые правильные данные
5. Покажет статистику

## 📊 Категории после импорта

| Категория | Тестов |
|-----------|--------|
| ФОНЕТИКА ва ҲОДИСАҲОИ ФОНЕТИКӢ | 97 |
| ИМЛО | 62 |
| ЛЕКСИКА | 308 |
| ФРАЗЕОЛОГИЯ | 196 |
| МОРФОЛОГИЯ | 165 |
| СИНТАКСИС | 192 |
| АДАБИЁТ | 154 |

## ⚠️ Важно

- Subject "Забони тоҷикӣ" с ID=1 **НЕ удаляется**
- Удаляются только Topics и Tasks
- Требуется подтверждение перед удалением

## 🔍 Проверка после импорта

```python
python manage.py shell

from core.models import Subject, Topic, Task

subject = Subject.objects.get(id=1)
print(f"Subject: {subject.title} (ID: {subject.id})")
print(f"Topics: {Topic.objects.filter(subject=subject).count()}")
print(f"Tasks: {Task.objects.filter(subject=subject).count()}")
```

## 📁 Необходимые файлы

В директории должны быть:
- ✅ `test_database_fixed.json`
- ✅ `answer_keys.json`

Все готово к запуску! 🎉
