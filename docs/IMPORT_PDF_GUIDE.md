# 📚 Руководство по импорту заданий из PDF

## 🎯 Назначение

Этот скрипт позволяет автоматически импортировать тестовые задания из PDF файлов в базу данных Django.

## 📋 Требования

```bash
pip install PyPDF2
```

## 🚀 Использование

### ⚠️ ШАГ 1: Предпросмотр данных (ОБЯЗАТЕЛЬНО!)

**Перед импортом всегда проверяйте корректность парсинга:**

```bash
python manage.py preview_pdf_tasks A2-12_Math_ru.pdf --limit 5
```

Эта команда покажет:
- ✅ Количество извлеченных символов
- ✅ Первые 500 символов текста
- ✅ Статистику по темам
- ✅ Примеры первых 5 заданий

**Проверьте:**
- Правильно ли распознаны вопросы
- Все ли варианты ответов на месте
- Корректно ли определены темы

### ШАГ 2: Импорт в базу данных

**Метод 1: Django Management Command (Рекомендуется)**

```bash
python manage.py import_pdf_tasks A2-12_Math_ru.pdf --subject "Математика"
```

**Метод 2: Standalone скрипт**

```bash
python import_tasks_from_pdf.py
```

## 📝 Формат PDF

Скрипт ожидает следующую структуру в PDF:

```
НАЗВАНИЕ ТЕМЫ

1  Текст вопроса
A) Вариант 1
B) Вариант 2
C) Вариант 3
D) Вариант 4

2  Следующий вопрос
A) Вариант 1
B) Вариант 2
C) Вариант 3
D) Вариант 4
```

## 🔧 Что делает скрипт

1. **Читает PDF файл** - извлекает текст из всех страниц
2. **Парсит задания** - находит номера, вопросы и варианты ответов
3. **Определяет темы** - группирует задания по темам
4. **Сохраняет в БД** - создает записи в моделях Subject, Topic, Task

## 📊 Структура данных

### Subject (Предмет)
- `title`: Математика
- `description`: Подготовка к ЕГЭ
- `icon`: 📐

### Topic (Тема)
- `title`: Действия с рациональными и иррациональными числами
- `subject`: Математика
- `difficulty`: easy
- `order`: 1

### Task (Задание)
- `topic`: Ссылка на тему
- `question`: Полный текст вопроса с вариантами
- `correct_answer`: "1", "2", "3" или "4"
- `difficulty`: easy
- `points`: 5

## ⚠️ Важные замечания

### Правильные ответы

По умолчанию скрипт устанавливает `correct_answer = "1"` для всех заданий.

**Вам нужно вручную обновить правильные ответы:**

1. Через Django Admin: http://localhost:8000/admin/core/task/
2. Или через скрипт обновления (создайте отдельно)

### Формат вопросов

Вопросы сохраняются в формате:

```
Текст вопроса

1. Вариант A
2. Вариант B
3. Вариант C
4. Вариант D
```

Пользователь выбирает номер (1-4) в качестве ответа.

## 🔍 Проверка импорта

После импорта проверьте:

```bash
python manage.py shell
```

```python
from core.models import Subject, Topic, Task

# Проверяем предметы
Subject.objects.all()

# Проверяем темы
Topic.objects.filter(subject__title='Математика')

# Проверяем задания
Task.objects.filter(topic__title__icontains='рациональными')

# Количество заданий
Task.objects.count()
```

## 🐛 Отладка

Если задания не импортируются:

1. **Проверьте формат PDF**
   ```python
   python manage.py import_pdf_tasks A2-12_Math_ru.pdf
   ```
   Скрипт покажет первые 500 символов извлеченного текста

2. **Проверьте кодировку**
   - PDF должен содержать текст, а не изображения
   - Используйте PDF с возможностью выделения текста

3. **Проверьте паттерны**
   - Номера заданий должны быть в формате: `1 `, `2 `, и т.д.
   - Варианты должны быть: `A)`, `B)`, `C)`, `D)` или `А)`, `Б)`, `В)`, `Г)`

## 📈 Расширение функционала

### Добавление правильных ответов из PDF

Если в PDF есть раздел с ответами, создайте функцию:

```python
def extract_answers_from_pdf(text):
    """Извлекает правильные ответы из раздела 'Ответы'"""
    answers = {}
    # Ваша логика парсинга
    return answers
```

### Поддержка других форматов

Для поддержки других форматов вопросов, измените регулярные выражения в функции `parse_tasks_from_text()`.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи импорта
2. Убедитесь, что PyPDF2 установлен
3. Проверьте права доступа к PDF файлу
4. Убедитесь, что база данных мигрирована: `python manage.py migrate`

## ✅ Пример успешного импорта

```
📖 Чтение PDF файла: A2-12_Math_ru.pdf
✅ Извлечено 15420 символов
🔍 Парсинг заданий...
  ✓ Задание #1: Если число 48 увеличить в четыре раза, оно увеличится на...
  ✓ Задание #2: Если число 72 уменьшить в три раза, оно уменьшится на...
✅ Найдено 25 заданий
💾 Сохранение в базу данных...
✅ Создан предмет: Математика
✅ Создана тема: Действия с рациональными и иррациональными числами
  📝 Тема 'Действия с рациональными и иррациональными числами': создано 25, пропущено 0
✅ Импорт завершен!
```
