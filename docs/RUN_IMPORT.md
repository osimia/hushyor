# 🚀 Инструкция по импорту тестов

## ⚠️ ВАЖНО: Изменения в модели Task

Добавлено новое поле `original_test_id` для хранения исходного ID из JSON.
Теперь Django будет автоматически генерировать уникальные ID для Task, избегая конфликтов с другими Subject'ами.

## 📝 Шаги для импорта:

### 1. Активировать виртуальное окружение
```bash
cd /home/osimi/Рабочий\ стол/projects/hushyor
source my_env/bin/activate
```

### 2. Создать и применить миграцию
```bash
python manage.py makemigrations
python manage.py migrate
```

### 3. Запустить импорт
```bash
python manage.py clean_and_import_tests
```

## 📊 Что произойдет:

1. ✅ Subject "Забони тоҷикӣ" (ID: 1) останется
2. 🗑️ Удалятся все Topics и Tasks этого Subject
3. ✨ Создадутся 7 новых Topics
4. 📥 Импортируются 919 тестов:
   - **907 тестов** с ответами
   - **12 тестов** без ответов (correct_answer будет пустым)
5. 🆔 Django автоматически назначит новые уникальные ID
6. 💾 Исходные ID сохранятся в поле `original_test_id`

## 🔍 Проверка после импорта:

```bash
python manage.py shell
```

```python
from core.models import Subject, Topic, Task

# Проверка Subject
s = Subject.objects.get(title="Забони тоҷикӣ")
print(f"Subject ID: {s.id}")

# Проверка Topics
print(f"Topics: {Topic.objects.filter(subject=s).count()}")

# Проверка Tasks
print(f"Tasks: {Task.objects.filter(subject=s).count()}")

# Пример Task с original_test_id
task = Task.objects.filter(subject=s).first()
print(f"Task ID: {task.id}, Original ID: {task.original_test_id}")
```

## ⚠️ Тесты без ответов (12 штук):

ID: 52, 81, 203, 234, 359, 390, 515, 546, 671, 702, 827, 858

Эти тесты будут импортированы с пустым `correct_answer`.
Вы сможете добавить ответы позже через Django admin.
