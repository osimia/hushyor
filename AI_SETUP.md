# Настройка ИИ-помощника с Gemini API

## 📋 Что реализовано

ИИ-помощник на базе Google Gemini с тремя функциями:

1. **Теория** - генерирует короткий урок с примерами по задаче
2. **Подсказка** - дает первый шаг решения задачи
3. **Чат** - отвечает на произвольные вопросы пользователя

## 🚀 Установка

### Шаг 1: Установите зависимости

```bash
pip install -r requirements.txt
```

Это установит:
- `google-generativeai` - SDK для работы с Gemini API
- `python-dotenv` - для загрузки переменных окружения

### Шаг 2: Получите API ключ Gemini

1. Перейдите на https://makersuite.google.com/app/apikey
2. Войдите с Google аккаунтом
3. Нажмите "Create API Key"
4. Скопируйте полученный ключ

### Шаг 3: Настройте .env файл

Откройте файл `.env` и замените:

```env
GEMINI_API_KEY="your-gemini-api-key-here"
```

На ваш реальный ключ:

```env
GEMINI_API_KEY="AIzaSyD..."
```

## 🎯 Как использовать

### В интерфейсе задачи:

1. **Кнопка "Теория"** - нажмите, чтобы получить урок с теорией и примерами
2. **Кнопка "Подсказка"** - нажмите, чтобы получить первый шаг решения
3. **Поле ввода** - задайте любой вопрос по задаче

### Примеры работы:

**Теория:**
- Объясняет основную концепцию
- Приводит 1-2 примера с решением
- Дает практический совет

**Подсказка:**
- С чего начать решение
- Какую формулу использовать
- На что обратить внимание

**Чат:**
- Отвечает на вопросы о задаче
- Направляет к решению, не давая прямой ответ
- Объясняет непонятные моменты

## 🔧 Техническая информация

### Файлы:

- `core/ai_helper.py` - модуль с функциями для работы с Gemini API
- `core/views.py` - обработка запросов к ИИ
- `core/templates/task.html` - интерфейс ИИ-помощника

### Функции в ai_helper.py:

```python
get_theory_lesson(task_question, task_subject)  # Генерация теории
get_hint(task_question, task_subject)           # Генерация подсказки
get_ai_response(user_message, task_question, task_subject)  # Ответ на вопрос
```

## ⚠️ Важно

- **Без API ключа** ИИ-помощник будет показывать предупреждение
- **Лимиты API** - бесплатный план Gemini имеет лимиты запросов
- **Интернет** - для работы требуется подключение к интернету

## 🐛 Решение проблем

### "API ключ не настроен"
- Проверьте, что в `.env` указан правильный ключ
- Убедитесь, что ключ не равен `"your-gemini-api-key-here"`

### "Ошибка при генерации"
- Проверьте интернет-соединение
- Убедитесь, что API ключ валиден
- Проверьте лимиты на вашем API ключе

### Ошибки импорта
- Убедитесь, что установили все зависимости: `pip install -r requirements.txt`
- Перезапустите сервер Django

## 📝 Примечания

- Ответы генерируются в реальном времени
- Каждый запрос занимает 2-5 секунд
- ИИ настроен на русский язык
- Промпты оптимизированы для образовательных целей
