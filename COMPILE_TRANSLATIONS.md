# Компиляция переводов

## Быстрый старт

После настройки Django i18n необходимо скомпилировать файлы переводов:

```bash
# Активируйте виртуальное окружение
.\venv\Scripts\activate

# Скомпилируйте переводы
python manage.py compilemessages
```

## Если gettext не установлен

### Windows:
1. Скачайте gettext для Windows: https://mlocati.github.io/articles/gettext-iconv-windows.html
2. Установите и добавьте в PATH
3. Перезапустите терминал
4. Запустите `python manage.py compilemessages`

### Альтернатива (без gettext):
Файлы `.mo` можно создать вручную или использовать онлайн-конвертер .po → .mo

## Проверка работы

1. Запустите сервер: `python manage.py runserver`
2. Откройте сайт: http://localhost:8000
3. Переключите язык через меню в навигации (🇹🇯 Тоҷикӣ / 🇷🇺 Русский)
4. Интерфейс должен измениться на выбранный язык

## Что реализовано

✅ Django i18n настроен (settings.py)
✅ Файлы переводов созданы (locale/tg/ и locale/ru/)
✅ Шаблон task.html переведен с тегами {% trans %}
✅ Переключатель языков добавлен в навигацию
✅ Таджикский язык установлен по умолчанию

## Следующие шаги

Для полного перевода всего сайта нужно:
1. Добавить теги `{% load i18n %}` и `{% trans %}` в остальные шаблоны
2. Обновить файлы .po с новыми переводами
3. Скомпилировать: `python manage.py compilemessages`

## Структура переводов

```
locale/
├── tg/LC_MESSAGES/
│   ├── django.po  ← Редактируйте здесь переводы на таджикский
│   └── django.mo  ← Генерируется автоматически
└── ru/LC_MESSAGES/
    ├── django.po  ← Редактируйте здесь переводы на русский
    └── django.mo  ← Генерируется автоматически
```
