# 📱 Настройка Open Graph для Facebook-шаринга задач

## ✅ Что уже настроено

### 1. Динамические мета-теги для каждой задачи
Каждая задача теперь имеет уникальные Open Graph теги:
- **URL**: `https://hushyor.com/task/<id>/`
- **Заголовок**: "Задание №X по [предмет] | hushyor"
- **Описание**: Первые 150 символов текста вопроса
- **Изображение**: Автоматически генерируется с текстом вопроса

### 2. Кнопка "Поделиться в Facebook"
На каждой странице задачи есть синяя кнопка с иконкой Facebook, которая открывает Facebook Sharer.

### 3. Автоматическая генерация изображений
Система автоматически создает красивые карточки 1200×630px с:
- Градиентным фоном (синий → фиолетовый)
- Логотипом "hushyor"
- Названием предмета
- Текстом вопроса (до 8 строк)
- Футером с призывом к действию

---

## 🚀 Как это работает

### Технический стек
- **Pillow** (PIL) — генерация изображений
- **Django view** — `/task/<id>/og-image.png`
- **Кэширование** — 24 часа (для производительности)

### Файлы
1. **`core/og_image_generator.py`** — логика генерации изображений
2. **`core/views.py`** — view `task_og_image_view()`
3. **`backend/urls.py`** — маршрут `/task/<id>/og-image.png`
4. **`core/templates/task.html`** — OG-теги и кнопка Facebook

---

## 🧪 Как проверить работу

### 1. Facebook Sharing Debugger
Это официальный инструмент Facebook для проверки Open Graph:

1. Открой: https://developers.facebook.com/tools/debug/
2. Вставь URL задачи: `https://hushyor.com/task/1/`
3. Нажми **"Debug"**
4. Увидишь превью карточки, как её видит Facebook
5. Если что-то не так — нажми **"Scrape Again"** для обновления кэша

### 2. Проверка в реальном Facebook
1. Открой Facebook (или Messenger)
2. Вставь ссылку на задачу в пост/сообщение
3. Facebook автоматически создаст красивую карточку

### 3. Проверка изображения напрямую
Открой в браузере: `https://hushyor.com/task/1/og-image.png`
Должна загрузиться картинка с текстом вопроса.

---

## 📦 Установка зависимостей

Pillow уже добавлен в `requirements.txt`:
```txt
Pillow>=12.0.0
```

После деплоя на Railway убедись, что зависимости установлены:
```bash
pip install -r requirements.txt
```

---

## 🎨 Кастомизация изображений

### Изменить цвета градиента
Отредактируй `core/og_image_generator.py`, строки 23-28:
```python
# Рисуем градиент (от синего к фиолетовому)
for y in range(height):
    ratio = y / height
    r = int(79 + (138 - 79) * ratio)   # Red
    g = int(109 + (43 - 109) * ratio)  # Green
    b = int(245 + (226 - 245) * ratio) # Blue
```

### Изменить шрифты
Строки 33-40 в `og_image_generator.py`:
```python
title_font = ImageFont.truetype('/путь/к/шрифту.ttf', 48)
question_font = ImageFont.truetype('/путь/к/шрифту.ttf', 36)
```

### Изменить текст футера
Строка 78:
```python
footer_text = "Проверь свои знания на hushyor.com"
```

---

## 🐛 Возможные проблемы

### Проблема: Шрифты не загружаются
**Решение**: Система использует fallback на дефолтный шрифт. Для лучшего качества установи DejaVu шрифты на сервере:
```bash
apt-get install fonts-dejavu-core
```

### Проблема: Facebook показывает старую картинку
**Решение**: Используй Facebook Sharing Debugger и нажми "Scrape Again" для очистки кэша.

### Проблема: Изображение не генерируется
**Решение**: Проверь логи Django на наличие ошибок:
```bash
python manage.py runserver
# Открой /task/1/og-image.png и смотри консоль
```

---

## 📊 Производительность

### Кэширование
Изображения кэшируются на 24 часа через HTTP-заголовок:
```python
response['Cache-Control'] = 'public, max-age=86400'
```

### Оптимизация
- Изображения сохраняются с `optimize=True`
- Генерация происходит только при первом запросе
- Браузеры и CDN кэшируют картинки автоматически

---

## 🔗 Полезные ссылки

- **Facebook Sharing Debugger**: https://developers.facebook.com/tools/debug/
- **Open Graph Protocol**: https://ogp.me/
- **Pillow Documentation**: https://pillow.readthedocs.io/

---

## 📝 Примеры использования

### Поделиться задачей программно
```html
<a href="https://www.facebook.com/sharer/sharer.php?u=https://hushyor.com/task/123/" 
   target="_blank">
   Поделиться в Facebook
</a>
```

### Получить OG-изображение для задачи
```python
from core.og_image_generator import generate_task_og_image
from core.models import Task

task = Task.objects.get(id=1)
image_buffer = generate_task_og_image(task)
# image_buffer — это BytesIO с PNG
```

---

## ✨ Что дальше?

### Возможные улучшения:
1. **Добавить логотип** — вместо текста "hushyor" использовать PNG-логотип
2. **Разные фоны** — для разных предметов разные цвета
3. **Статистика** — показывать на картинке "Решили X человек"
4. **Кэш в Redis** — для ещё большей производительности
5. **Watermark** — добавить QR-код или защиту от копирования

---

**Автор**: hushyor team  
**Дата**: 2026-01-06
