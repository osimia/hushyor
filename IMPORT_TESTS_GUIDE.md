# Руководство по импорту тестов в БД

Это универсальное руководство описывает процесс импорта тестов для любого предмета в базу данных проекта hushyor.

## Оглавление

1. [Структура данных](#структура-данных)
2. [Подготовка данных](#подготовка-данных)
3. [Парсинг тестов из Markdown](#парсинг-тестов-из-markdown)
4. [Импорт в БД](#импорт-в-бд)
5. [Примеры использования](#примеры-использования)
6. [Решение проблем](#решение-проблем)

---

## Структура данных

### Модели Django

В проекте используются следующие модели:

- **Subject** (Предмет) - математика, таджикский язык, история и т.д.
- **Topic** (Тема) - разделы внутри предмета
- **Task** (Тест) - конкретные задания с вариантами ответов

### Формат JSON для импорта

```json
{
  "subject": "Название предмета",
  "topics": [
    {
      "title": "Название темы",
      "order": 1,
      "tasks": [
        {
          "original_test_id": 1,
          "question": "Текст вопроса",
          "options": {
            "A": "Вариант A",
            "B": "Вариант B",
            "C": "Вариант C",
            "D": "Вариант D"
          },
          "correct_answer": "A",
          "difficulty": 1
        }
      ]
    }
  ]
}
```

---

## Подготовка данных

### Шаг 1: Исходные файлы

Вам понадобятся два файла:

1. **Файл с тестами** (например, `Subject_Tests.md`) - содержит вопросы и варианты ответов
2. **Файл с ключами** (например, `Subject_Tests_key.md`) - содержит правильные ответы

### Формат файла с тестами

```markdown
\section*{НАЗВАНИЕ ТЕМЫ}

1 Текст первого вопроса?
A) Вариант A
B) Вариант B
C) Вариант C
D) Вариант D

2 Текст второго вопроса?
A) Вариант A
B) Вариант B
C) Вариант C
D) Вариант D
```

### Формат файла с ключами

```markdown
\begin{tabular}{|l|l|}
\hline 1 & A \\
\hline 2 & B \\
\hline 3 & C \\
\end{tabular}
```

---

## Парсинг тестов из Markdown

### Использование скрипта parse_math_tests.py

Скрипт `parse_math_tests.py` парсит Markdown файлы и создает JSON для импорта.

#### Структура скрипта

```python
#!/usr/bin/env python3
import re
import json

def parse_answer_key(key_file_path):
    """Парсинг файла с ответами"""
    answers = {}
    with open(key_file_path, 'r', encoding='utf-8') as f:
        content = f.read()
    pattern = r'\\hline\s+(\d+)\s+&\s+([A-D])\s+\\\\'
    matches = re.findall(pattern, content)
    for num, answer in matches:
        answers[int(num)] = answer
    return answers

def parse_tests(test_file_path, answers):
    """Парсинг файла с тестами"""
    # Логика парсинга тестов
    pass

def assign_topics_to_tasks(tasks):
    """Распределение тестов по темам"""
    topics_data = [
        {
            'title': 'НАЗВАНИЕ ТЕМЫ 1',
            'order': 1,
            'start': 1,
            'end': 50
        },
        # ... другие темы
    ]
    # Логика распределения
    pass
```

#### Адаптация под новый предмет

1. **Скопируйте** `parse_math_tests.py` и переименуйте (например, `parse_history_tests.py`)

2. **Измените функцию** `assign_topics_to_tasks()`:
   ```python
   topics_data = [
       {
           'title': 'История Древнего мира',
           'order': 1,
           'start': 1,
           'end': 100
       },
       {
           'title': 'Средневековье',
           'order': 2,
           'start': 101,
           'end': 200
       }
   ]
   ```

3. **Обновите** `main()` функцию:
   ```python
   def main():
       test_file = 'History_Tests.md'
       key_file = 'History_Tests_key.md'
       output_file = 'history_tests_import.json'
       
       # Остальной код остается без изменений
   ```

4. **Запустите скрипт**:
   ```bash
   python3 parse_history_tests.py
   ```

---

## Импорт в БД

### Django Management Command

Используйте команду `import_math_from_json` для импорта данных в БД.

### Синтаксис команды

```bash
python manage.py import_math_from_json <json_file> [опции]
```

### Параметры

- `json_file` - путь к JSON файлу с тестами (обязательный)
- `--subject` - название предмета (если не указано, берется из JSON)
- `--clear` - очистить существующие тесты и темы предмета перед импортом
- `--dry-run` - показать что будет импортировано без сохранения в БД

### Примеры команд

#### Базовый импорт
```bash
python manage.py import_math_from_json math_tests_import.json
```

#### Импорт с указанием предмета
```bash
python manage.py import_math_from_json history_tests_import.json --subject "История"
```

#### Импорт с очисткой старых данных
```bash
python manage.py import_math_from_json math_tests_import.json --clear
```

#### Проверка данных без импорта
```bash
python manage.py import_math_from_json math_tests_import.json --dry-run
```

---

## Примеры использования

### Пример 1: Импорт математических тестов

```bash
# 1. Парсинг тестов
python3 parse_math_tests.py

# Вывод:
# Парсинг ответов...
# Найдено 692 ответов
# Парсинг тестов...
# Найдено 570 тестов
# ✅ Данные сохранены в math_tests_import.json

# 2. Проверка данных
python manage.py import_math_from_json math_tests_import.json --dry-run

# 3. Импорт в БД
python manage.py import_math_from_json math_tests_import.json

# Вывод:
# ✅ Найден предмет: Математика
# ✅ Создана тема: АМАЛХО БО АДАДХОИ РАТСИОНАЛЙ ВА ИРРАТСИОНАЛЙ
#   📝 Импортировано тестов: 47
# ✅ Импорт завершен успешно!
```

### Пример 2: Импорт тестов по истории

```bash
# 1. Создайте parse_history_tests.py на основе parse_math_tests.py
# 2. Адаптируйте темы под историю
# 3. Запустите парсинг
python3 parse_history_tests.py

# 4. Импортируйте в БД
python manage.py import_math_from_json history_tests_import.json --subject "История"
```

### Пример 3: Переимпорт с очисткой

```bash
# Если нужно полностью заменить тесты предмета
python manage.py import_math_from_json math_tests_import.json --clear
```

---

## Решение проблем

### Проблема: Найдено мало тестов

**Причина**: Неправильный формат Markdown или проблемы с кодировкой

**Решение**:
1. Проверьте кодировку файлов (должна быть UTF-8)
2. Убедитесь, что варианты ответов начинаются с `A)`, `B)`, `C)`, `D)`
3. Проверьте, что номера вопросов идут последовательно

### Проблема: Тесты импортируются в неправильные темы

**Причина**: Неверные диапазоны в `topics_data`

**Решение**:
1. Откройте исходный Markdown файл
2. Найдите номера вопросов для каждой темы
3. Обновите диапазоны `start` и `end` в функции `assign_topics_to_tasks()`

### Проблема: Дублирование тестов

**Причина**: Повторный импорт без флага `--clear`

**Решение**:
```bash
# Вариант 1: Импорт с очисткой
python manage.py import_math_from_json math_tests_import.json --clear

# Вариант 2: Удалить тесты вручную через Django admin
```

### Проблема: Математические формулы отображаются неправильно

**Причина**: LaTeX формулы не обрабатываются

**Решение**:
1. В скрипте парсинга формулы сохраняются как `[ФОРМУЛА: ...]`
2. На фронтенде используйте библиотеку MathJax или KaTeX для рендеринга
3. Или обработайте формулы вручную перед импортом

---

## Чеклист импорта нового предмета

- [ ] Подготовлены файлы с тестами и ключами в формате Markdown
- [ ] Скопирован и адаптирован скрипт парсинга
- [ ] Определены темы и их диапазоны вопросов
- [ ] Запущен парсинг и создан JSON файл
- [ ] Проверены данные через `--dry-run`
- [ ] Выполнен импорт в БД
- [ ] Проверены импортированные тесты в Django admin
- [ ] Протестирована работа на фронтенде

---

## Структура файлов проекта

```
hushyor/
├── core/
│   ├── management/
│   │   └── commands/
│   │       ├── import_math_from_json.py  # Команда импорта
│   │       └── ...
│   └── models.py                          # Модели Subject, Topic, Task
├── parse_math_tests.py                    # Скрипт парсинга математики
├── math_tests_import.json                 # JSON для импорта
├── A2-12_Math_tj.md                       # Исходные тесты
├── A2-12_Math_tj_key.md                   # Ключи ответов
└── IMPORT_TESTS_GUIDE.md                  # Эта документация
```

---

## Дополнительные возможности

### Массовый импорт нескольких предметов

Создайте bash скрипт:

```bash
#!/bin/bash
# import_all_subjects.sh

echo "Импорт всех предметов..."

python manage.py import_math_from_json math_tests_import.json
python manage.py import_math_from_json history_tests_import.json --subject "История"
python manage.py import_math_from_json tajik_tests_import.json --subject "Таджикский язык"

echo "✅ Все предметы импортированы!"
```

### Экспорт тестов из БД

Для создания резервной копии или переноса данных:

```python
# export_tests.py
from core.models import Subject, Topic, Task
import json

subject = Subject.objects.get(title='Математика')
data = {
    'subject': subject.title,
    'topics': []
}

for topic in subject.topics.all():
    topic_data = {
        'title': topic.title,
        'order': topic.order,
        'tasks': []
    }
    for task in topic.tasks.all():
        topic_data['tasks'].append({
            'original_test_id': task.original_test_id,
            'question': task.question,
            'options': task.options,
            'correct_answer': task.correct_answer,
            'difficulty': task.difficulty
        })
    data['topics'].append(topic_data)

with open('export.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
```

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи Django
2. Используйте `--dry-run` для диагностики
3. Проверьте формат исходных файлов
4. Обратитесь к разработчикам проекта

**Удачного импорта! 🚀**
