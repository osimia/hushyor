# 🔄 Очистка и импорт правильных данных

## 📋 Что делает скрипт

Скрипт `clean_and_import.py` выполняет:

1. **🧹 Очистка старых данных**
   - Удаляет все Topics для Subject "Забони тоҷикӣ"
   - Удаляет все Tasks для Subject "Забони тоҷикӣ"
   - Subject "Забони тоҷикӣ" остается (не удаляется)

2. **📥 Импорт новых данных**
   - Создает 7 правильных Topics (категорий)
   - Импортирует ~1159 тестов с правильными категориями
   - Связывает тесты с правильными ответами

## 🚀 Запуск

```bash
cd /home/osimi/Рабочий\ стол/projects/hushyor
python3 clean_and_import.py
```

## ⚠️ Важно!

- Скрипт **попросит подтверждение** перед удалением
- Введите `yes` для продолжения
- Все старые Topics и Tasks будут удалены
- Subject "Забони тоҷикӣ" останется

## 📊 Результат

После выполнения в БД будет:

```
Subject: "Забони тоҷикӣ"
├── Topic: "ФОНЕТИКА ва ҲОДИСАҲОИ ФОНЕТИКӢ" (97 тестов)
├── Topic: "ИМЛО" (62 теста)
├── Topic: "ЛЕКСИКА" (308 тестов)
├── Topic: "ФРАЗЕОЛОГИЯ" (196 тестов)
├── Topic: "МОРФОЛОГИЯ" (165 тестов)
├── Topic: "СИНТАКСИС" (192 теста)
└── Topic: "АДАБИЁТ" (154 теста)
```

## 🔍 Проверка после импорта

```python
python manage.py shell

from core.models import Subject, Topic, Task

# Получаем Subject
subject = Subject.objects.get(title="Забони тоҷикӣ")

# Проверяем Topics
Topic.objects.filter(subject=subject).count()  # 7

# Проверяем Tasks
Task.objects.filter(subject=subject).count()  # ~1159

# Распределение по категориям
for topic in Topic.objects.filter(subject=subject):
    count = Task.objects.filter(topic=topic).count()
    print(f"{topic.title}: {count}")
```

## 📁 Необходимые файлы

Убедитесь что в директории есть:
- ✅ `test_database_fixed.json` - Тесты с правильными категориями
- ✅ `answer_keys.json` - Правильные ответы

## 🎯 Преимущества

- ✅ Нет путаницы со старыми данными
- ✅ Все категории правильные (из PDF)
- ✅ Безопасное удаление с подтверждением
- ✅ Полная статистика после импорта

## ❓ Если что-то пошло не так

### Откат изменений

Если нужно вернуть старые данные, используйте Django admin или shell для восстановления из бэкапа.

### Повторный запуск

Скрипт можно запускать повторно - он безопасно перезапишет данные.

## 📞 Поддержка

При ошибках проверьте:
- ✅ Django запущен
- ✅ Файлы `test_database_fixed.json` и `answer_keys.json` существуют
- ✅ Subject "Забони тоҷикӣ" существует в БД
